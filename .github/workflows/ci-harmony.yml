name: CI - HarmonyOS

on:
  push:
    branches: [main]
    paths:
      - 'core/**'
      - 'platforms/harmony/**'
      - 'scripts/harmony/**'
      - 'scripts/common/**'
      - '.github/workflows/ci-harmony.yml'
  pull_request:
    branches: [main]
    paths:
      - 'core/**'
      - 'platforms/harmony/**'
      - 'scripts/harmony/**'
      - 'scripts/common/**'
      - '.github/workflows/ci-harmony.yml'
  workflow_dispatch:
    inputs:
      build_mode:
        description: 'Build mode'
        required: false
        default: 'release'
        type: choice
        options:
          - release
          - debug

concurrency:
  group: ci-harmony-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: HarmonyOS Build
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Download HarmonyOS Command Line Tools
        run: |
          # Fetch the latest release asset URL from harmonyos-dev/hos-sdk
          RELEASE_JSON=$(curl -s -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/harmonyos-dev/hos-sdk/releases/latest)
          ASSET_URL=$(echo "$RELEASE_JSON" | jq -r '.assets[] | select(.name | contains("commandline-tools-mac")) | .browser_download_url')
          if [[ -z "$ASSET_URL" || "$ASSET_URL" == "null" ]]; then
            echo "::error::Failed to find commandline-tools-mac asset from hos-sdk releases"
            exit 1
          fi
          echo "Downloading: $ASSET_URL"
          curl -L -o commandline-tools.zip "$ASSET_URL"
          unzip -q commandline-tools.zip
          echo "Command Line Tools downloaded and extracted"

      - name: Install HarmonyOS SDK
        run: |
          # Set locale to CN so sdkmgr can reach Huawei's CDN
          export JAVA_TOOL_OPTIONS="$JAVA_TOOL_OPTIONS -Duser.country=CN"

          chmod +x command-line-tools/sdkmanager/bin/sdkmgr
          # Install HarmonyOS toolchains (compiler, native NDK, system libs)
          command-line-tools/sdkmanager/bin/sdkmgr install toolchains:9 OpenHarmony/toolchains:9 --accept-license

      - name: Configure build environment
        run: |
          CLT_ROOT="$(pwd)/command-line-tools"
          echo "DEVECO_HOME=${CLT_ROOT}" >> $GITHUB_ENV
          echo "DEVECO_SDK_HOME=${CLT_ROOT}/sdk" >> $GITHUB_ENV
          echo "HOS_SDK_HOME=$(pwd)/hwsdk" >> $GITHUB_ENV

      - name: Build HarmonyOS HAR
        run: ./scripts/harmony/build.sh --mode ${{ inputs.build_mode || 'release' }}

      - name: Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: harmony-har-${{ github.sha }}
          path: dist/harmony/
          retention-days: 7
